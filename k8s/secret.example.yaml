apiVersion: v1
kind: Secret
metadata:
  name: llm-guard-secret
type: Opaque
# Security: SENSITIVE values go in a Secret, never a ConfigMap.
# This file is an EXAMPLE — do not commit real secrets to git.
# In production, create the Secret with `kubectl create secret ...`,
# or use Sealed Secrets / External Secrets / a vault.
stringData:
  GUARD_SYSTEM_PROMPT_CANARY: "change-me-to-a-secret-marker"
